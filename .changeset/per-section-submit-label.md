---
"form-page-builder": minor
---

Sections can now override the submit button's text, not just its color/size — editable from the same per-section submit controls in the canvas. Falls back to the form's global submit label when unset.
