---
"form-page-builder": minor
---

Increased the default `theme.layout.maxWidth` from 640 to 1200 for a more usable full-width layout out of the box. Consumers who already set `theme.layout.maxWidth` (directly or via `themeOverrides`) are unaffected.
