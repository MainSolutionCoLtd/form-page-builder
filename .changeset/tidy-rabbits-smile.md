---
"form-page-builder": minor
---

Add `features.deviceToggle` — hides the Laptop/Tablet/Mobile width switcher above the Preview canvas for embeds that don't need it, while it stays available (and defaults to on) for consumers that do. Preview renders at the Laptop (full) width when hidden.
